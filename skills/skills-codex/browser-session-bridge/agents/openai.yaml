interface:
  display_name: "Browser Session Bridge"
  short_description: "Use Codex with the user's signed-in Chrome"
  default_prompt: "Use $browser-session-bridge to complete this authenticated Chrome operation and verify the landed artifact."
