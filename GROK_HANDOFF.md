# Grok Goal Handoff: Dual-Runtime Business Research Acceptance

## Outcome to Deliver

Finish the remaining Grok acceptance gates for run `20260718T011517Z` without
regressing any existing shared or Codex pass. Completion means the root verifier
reports `PASS` for shared outputs, Codex, and Grok. A plan, browser click, model
claim, or unverified file is not completion.

## Verified Starting Point

- Shared P1, P2, P3, and P5: `PASS`.
- Codex P1-P5 and all six browser gates: `PASS`.
- Grok P1, P2, and P5: `PASS`.
- Grok P3 browser acquisition for CNKI, SSRN, ScienceDirect, and Wiley: `PASS`.
- Grok canonical P3 synthesis: `INCOMPLETE`.
- Grok P4 CNRDS and CSMAR real exports: `INCOMPLETE`.
- The official DevTools facade uses a dedicated persistent Chrome profile and
  exposes exactly 15 restricted tools. Do not substitute the legacy raw
  `chrome-mcp` path for primary acceptance.

Read these files before acting:

1. `task_plan.md`
2. `progress.md`
3. `findings.md`
4. `docs/BUSINESS_RESEARCH_E2E_ACCEPTANCE.md`
5. `skills/browser-session-bridge/SKILL.md`
6. `skills/browser-session-bridge/references/grok-chrome-devtools-mcp.md`
7. `skills/business-lit-review/SKILL.md`
8. `skills/method-harvest/SKILL.md`
9. `skills/cn-data-bridge/SKILL.md`

## Parallel Work Policy

Use native Grok subagents when available, but keep ownership disjoint:

- `p3_synthesis`: produce the independent blind canonical P3 synthesis from the
  frozen verified PDFs and inputs, then stop. It must not inspect verifier
  implementation or copy expected outputs.
- `p4_preflight`: inspect only the frozen CNRDS/CSMAR specs, prompts, safe facade,
  and external accept/promote chain; report drift or blockers without operating
  Chrome.
- `acceptance_audit`: run read-only mirror, focused tests, and verifier snapshots;
  report failures without editing artifacts generated by other workers.
- The leader owns all source edits, browser operations, promotion, planning-file
  updates, and final acceptance.

Only one actor may operate the dedicated Chrome profile at a time. Do not run
CNRDS and CSMAR browser sessions concurrently. The leader must serialize the two
real exports and keep each site's candidate, artifact, and receipt lineage
separate. If subagents are unavailable, execute the same work sequentially; that
is not a blocker.

## Remaining Work

### 1. Grok P3 canonical synthesis

- Use the frozen verified P3 PDFs and canonical skills to generate the blind
  synthesis artifacts and runtime receipt.
- Preserve `unknown` and `fail` where the source does not support a stronger
  claim. Do not repair or invent paper facts to satisfy a checker.
- Ensure each method card has source-page evidence and PDF lineage for required
  method, variable, measurement, and identification fields.
- Submit the result to the independent external verifier. Do not let the same
  generation process self-certify the receipt.

### 2. Grok P4 CNRDS real export

- Use the authenticated dedicated profile and start from:
  `https://www.cnrds.com/Home/Index#/FeaturedDatabase/DB/CIRD/`.
- Resolve the final dataset page with the narrow `/ViewName/` tab filter; broad
  domain matching is ambiguous because authenticated background tabs may exist.
- Export the frozen CIRD slice only: 上市公司专利申请情况, year 2020, code
  `000001`, with the two required business rows for 上市公司本身 and 集团公司合计.
- Accept and promote only through `scripts/accept_grok_browser_candidate.py` and
  `scripts/promote_grok_browser_candidate.py` after independent structure/content
  verification.

### 3. Grok P4 CSMAR real export

- Verify table `FS_Combas` before filters.
- Use global `重置` exactly once immediately after table identity. It clears
  dates and code, so never use it after applying filters. `全选` is a toggle and
  must not be used as a reset.
- Verify the mandatory 4/154 fields, apply the frozen date range and code
  `000001`, add only `A001000000` (资产总计) to reach 5/154, set `Typrep=A`,
  preview the exact one-row slice, and export.
- Use the same independent accept/promote chain as CNRDS.

### 4. Final release validation

- Synchronize/check the canonical-to-Codex mirror.
- Run the focused business suite in an isolated `uv` pytest environment.
- Run Node and shell syntax checks used by the existing checkpoint.
- Run the root verifier and require shared, Codex, and Grok to all be `PASS`.
- Review the final diff, preserve unrelated changes, and update
  `task_plan.md`, `progress.md`, and `findings.md` with evidence and remaining
  caveats.

Primary commands:

```bash
python3 tools/sync_business_portable_mirror.py --check
uv run --with pytest pytest -q \
  tests/test_accept_grok_browser_candidate.py \
  tests/test_browser_download_verifier.py \
  tests/test_business_browser_portability.py \
  tests/test_business_e2e_verifier.py \
  tests/test_business_literature_pipeline_e2e.py \
  tests/test_business_literature_synthesis_contract.py \
  tests/test_business_portable_mirror.py \
  tests/test_chrome_mcp_client.py \
  tests/test_cn_extract_verifier.py \
  tests/test_codex_skill_mirror.py \
  tests/test_devtools_mcp_facade.py \
  tests/test_grok_devtools_adapter_verifiers.py \
  tests/test_integrated_chain_verifier.py \
  tests/test_method_harvest_pdf_inspector.py \
  tests/test_p3_devtools_runtime_prompts.py \
  tests/test_p4_devtools_runtime_prompts.py \
  tests/test_p4_grok_runtime_prompts.py \
  tests/test_promote_grok_browser_candidate.py \
  tests/test_results_to_docx.py \
  tests/test_sciencedirect_recipe_contract.py
python3 scripts/verify_business_e2e.py --run-id 20260718T011517Z --json
git diff --check
```

## Safety and Evidence Boundaries

- Never print or persist credentials, cookies, auth headers, signed download
  URLs, tokens, or opaque browser references in chat, Markdown, or receipts.
- Saved-login submit buttons and ordinary site checkboxes may be operated under
  the user's standing authorization. Do not enter new credentials or automate
  sliders, image/audio CAPTCHA, MFA, QR, SMS, purchases, or irreversible actions.
- If a hard challenge appears, preserve the session and record one precise human
  action request. Do not weaken the acceptance gate or reuse an older artifact.
- Do not promote a learning run, a stale file, or an artifact whose identity,
  hash, mtime, schema, filters, and expected rows were not externally verified.
- Do not reopen completed Codex gates unless a source change invalidates their
  accepted hashes or tests.

## Completion Condition

Do not mark the Goal complete until this command exits successfully and its JSON
shows every required shared, Codex, and Grok gate as `PASS`:

```bash
python3 scripts/verify_business_e2e.py --run-id 20260718T011517Z --json
```

If blocked overnight, leave the Goal active with the exact failing gate, last
safe evidence, and next non-destructive action recorded in `progress.md`.
