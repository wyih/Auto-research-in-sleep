interface:
  display_name: "Method Harvest"
  short_description: "Extract grounded methods and measures from research fulltext."
  default_prompt: "Use $method-harvest to answer my method or measurement question from these papers, preserving source locations and unknowns; use the full card contract only when the downstream workflow requires it."
